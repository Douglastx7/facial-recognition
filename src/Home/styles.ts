import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerGeral: {
    backgroundColor: "gray",
  },
  container: {
    marginBottom: 200,
    width: "100%",
    height: 550,
  },
  camera: {
    flex: 1,
  },
});
